import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'ReviewCardsSlider Component | Feedback Evolution',
  description: 'Learn how to implement the ReviewCardsSlider component to showcase user feedback and reviews in an interactive slider format.',
  keywords: ['review cards', 'review slider', 'user feedback', 'interactive slider', 'react component']
}

export default function ReviewCardsSliderLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 